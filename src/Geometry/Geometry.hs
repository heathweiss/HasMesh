{- |
Supply imports for the Geometry modules

import qualified Geometry.Geometry as Geo
-}

module Geometry.Geometry(module Geometry.Vertex, module Geometry.ID) where

import Geometry.Vertex
import Geometry.ID
